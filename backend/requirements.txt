fastapi>=0.111
uvicorn[standard]>=0.30
pydantic>=2.7
pydantic-settings>=2.3
httpx>=0.27
orjson>=3.10
python-dotenv>=1.0

# Persistence (optional — activated when THRESHOLD_DATABASE_URL is set)
sqlalchemy[asyncio]>=2.0
asyncpg>=0.29

# Geospatial (serving the GeoJSON + spatial queries)
numpy>=1.26
pandas>=2.0
geopandas>=0.14
shapely>=2.0
pyproj>=3.6
pyogrio>=0.9
scikit-learn>=1.4
psycopg2-binary>=2.9

# Rate limiting
slowapi>=0.1.9

# AI agent framework (structured Gemini output)
pydantic-ai[google]>=0.0.46

# MCP server (auto-derived from FastAPI routes, mounted at /mcp)
fastmcp>=2.3
