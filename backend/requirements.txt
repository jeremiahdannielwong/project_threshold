fastapi>=0.111
uvicorn[standard]>=0.30
pydantic>=2.7
pydantic-settings>=2.3
httpx>=0.27
orjson>=3.10
python-dotenv>=1.0

# Persistence (optional — activated when THRESHOLD_DATABASE_URL is set)
sqlalchemy[asyncio]>=2.0
asyncpg>=0.29

# Data pipeline (`python -m app.pipeline` builds Tier A artifacts)
numpy>=1.26
pandas>=2.0
geopandas>=0.14
shapely>=2.0
pyproj>=3.6
fiona>=1.9
scikit-learn>=1.4

# Pipeline orchestration + tracking + validation + migrations
prefect>=3.0
mlflow>=2.18
pandera>=0.20
alembic>=1.13
psycopg2-binary>=2.9

# Testing
pytest>=8.2
pytest-asyncio>=0.23
anyio>=4.4
