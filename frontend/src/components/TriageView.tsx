/**
 * Deprecated. The triage workflow now lives inside the Watchlist tray
 * (LeftPanel.tsx), accessible from the layer rail or the `w` keyboard
 * shortcut. This file is retained only to satisfy the `View = 'Triage'`
 * type until that type is removed in a later cleanup.
 */
export default function TriageView() {
  return null;
}
